# Personal platform profile

This profile contains only deployment-specific policy and integration choices.
The executable semantics are owned by Agent Kernel.

Profile-owned choices include model/gateway selection, Windows Codex isolation,
Prometheus/Alertmanager endpoints, Feishu notification availability, Git/Docker
allowlists, and game-mode suppression.

`Framework != profile != Agent Kernel contract != runtime state`.
